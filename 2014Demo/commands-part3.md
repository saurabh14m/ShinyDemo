
### `includeHTML`
Include HTML, text, or rendered Markdown into a Shiny UI.

Usage
<pre><code>
includeHTML(path)

  includeText(path)

  includeMarkdown(path)

  includeCSS(path, ...)

  includeScript(path, ...)
</code></pre>  
